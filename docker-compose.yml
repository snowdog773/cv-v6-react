services:
  portfolio:
    build: .
    container_name: portfolio-site
    restart: unless-stopped
    ports:
      - "3000:80"
    networks:
      - proxy-nw

networks:
  proxy-nw:
    external: true
